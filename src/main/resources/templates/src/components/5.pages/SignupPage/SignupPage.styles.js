import {createGlobalStyle} from "styled-components";

const StyledSignupPage = createGlobalStyle`
    .article {
      margin-top: 156px;
      margin-bottom: 300px;
      display: flex;
      flex-direction: column;
      align-items: center;
      width: 100%;
    }
`;

export default StyledSignupPage;